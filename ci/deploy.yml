pages:
  extends: .base_config
  stage: deploy
  dependencies:
    - generate_report
  script:
    - mv reports/allure-report public
    - echo "Report is available at $CI_PAGES_URL"
  artifacts:
    paths:
      - public
  pages:
    path_prefix: $PATH_PREFIX
